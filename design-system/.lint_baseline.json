{
  "max_violations": 108,
  "_why": "v3.66 — 108 is `undesigned`: a hex in NO design token. The gate now also reports `unmigrated` (a hex that IS a token, written as a literal instead of var(--…)); that count is 68 and is not ratcheted here, because it is mechanical and because letting it into this number would mean a palette edit could erase violations with no CSS improving. Two things changed the figure from v3.46's 144. `_load_design_tokens()` was dead code — defined, documented, never called — so the gate had never read the design system it enforces, and the old note here claimed reconciling the palettes would lower this on its own, which it could not have. And the scan covered only results.html; video_review.html and timeline.html ship to users and were counted by nothing. Raise it again only with a reason here."
}
