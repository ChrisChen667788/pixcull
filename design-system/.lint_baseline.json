{
  "max_violations": 132,
  "_why": "v3.72 \u2014 132 undesigned, 19 unmigrated (was 131 / 81). The migration is the change: 62 literals became var() references, and video_review.html and timeline.html are at zero unmigrated. Undesigned rose by one rather than falling, and the reason is worth keeping: completing the accent ramp added --accent-mid and --accent-deep to BOTH themes, and the light theme's two values (#4d3401, #2b1c00) are in no design token because design-system/tokens.json does not model the light theme at all. That is Phase A.1, flagged in the file's own _themes placeholder since v0.9 and never done \u2014 until it is, every light-theme value the product ships counts here. #b5945f is now a token (color.brand.champagne-mid); it had been a literal in every three-stop brand gradient and never named. Raise it again only with a reason here."
}
